---
import Email from "../../../layouts/Email.astro";
const { slug } = Astro.params;
import fs from "fs/promises";

const file = await fs.readFile(`src/${slug}`, "utf-8");
---

<Email title={slug}>
  {file && <Fragment set:html={file} />}
</Email>
