---
import Layout from "../layouts/Layout.astro";
const { slug } = Astro.params;

const matches = import.meta.glob("../templates/html/**.html", {
  eager: true,
});

const Page = matches[`../${slug}`];

const Component = Page?.default;
---

<Layout>
  {Component && <Component />}
</Layout>
